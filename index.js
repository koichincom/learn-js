let message = "Hello, World!";
console.log (message);

const hello = "Hello!";
console.log (hello);

let array = ["one", "two", "three"];
console.log (array[0]);

let index = 0;
while(index < array.length){
    
    index++;
}